<Page x:Class="Launcher.Views.MainView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Launcher.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="MainView">

    <Grid>
        <Button x:Name="SettingsButton" Click="SettingsButton_Click"  HorizontalAlignment="Right"
                VerticalAlignment="Top" Margin="5" Content="Настройки"/>
        <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Button x:Name="StartButton" Click="StartButton_Click" Content="Начать без обновления" Margin="5"/>
            <Button x:Name="CheckButton" Click="CheckButton_Click" Margin="5"/>
        </StackPanel>
        <TextBlock x:Name="VersionText" VerticalAlignment="Bottom" HorizontalAlignment="Right" Margin="5"/>
    </Grid>
</Page>
